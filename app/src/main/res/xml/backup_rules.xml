<?xml version="1.0" encoding="utf-8"?><!--
   Sample backup rules file; uncomment and customize as necessary.
   See https://developer.android.com/guide/topics/data/autobackup
   for details.
   Note: This file is ignored for devices older than API 31
   See https://developer.android.com/about/versions/12/backup-restore
-->
<full-backup-content>
    <!--
   The Room DB ("workout_database") uses TRUNCATE journal mode (set in
   WorkoutDatabase.kt) so the single .db file is always self-consistent and
   safe for Auto Backup. Do NOT switch Room back to WAL without excluding the
   -wal/-shm sidecar files here, or restores can lose recent data.
-->
</full-backup-content>